apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-worker-evaluate
  labels:
    app: ai-worker-evaluate
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-worker-evaluate
  template:
    metadata:
      labels:
        app: ai-worker-evaluate
        tier: worker
    spec:
      containers:
        - name: worker-evaluate
          image: 209192769586.dkr.ecr.ap-northeast-2.amazonaws.com/scuad-ai-worker-evaluate:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: ai-shared-secret
          resources:
            requests:
              cpu: "250m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
