apiVersion: v1
kind: Secret
metadata:
  name: argocd-image-updater-git-secret
  namespace: argocd
type: Opaque
stringData:
  # GitHub PAT 또는 Deploy Key
  # 실제 값은 kubectl create secret 으로 생성하거나
  # 아래 값을 base64 인코딩하여 교체
  username: argocd-image-updater
  password: "GITHUB_PAT_HERE"
